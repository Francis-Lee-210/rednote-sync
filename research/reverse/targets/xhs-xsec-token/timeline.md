# Timeline

| 时间 | 角色 | 动作 | 结果 |
|---|---|---|---|
| 2026-08-20 | lead | master-route → PRIMARY=R3 js-reverse；读 precedent-auth / precedent-reverse / scope-contract | 路由命中 |
| 2026-08-20 | lead | case-init：手写 scope.md（本机无 pwsh，按契约允许手写） | ready_for_act=true |
| 2026-08-20 | cre | 拉取公开主页 HTML → 提取 3 个 JS bundle 地址 | HTTP 200，runtime/vendor/main |
| 2026-08-20 | cre | 下载 H5(ranchi)+PC(xhs-pc-web) 两组 bundle；静态定位 xsec_token：全部为读取透传，无生成代码 | E-002/E-003 |
| 2026-09-02 | lead | 用户授权以仓库外自注册测试小号完整 Cookie 建立隔离 profile；实验入口由裸 note_id 直链改为“搜索关键词 → 精确核对 note_id → 正常点击 → 捕获 token/source”。仅完成工具和 scope 修订，未执行本轮在线实验 | experiment/README.md、scope.md |
| 2026-09-02 | lead | 增加单命令总控脚本，自动串联 Cookie 登录、身份复核、搜索发现、精确点击、capture 和离线 token/source 定位；仍未执行在线实验 | experiment/scripts/run_experiment.py |
| 2026-08-20 | cre | 匿名 SSR：30 篇笔记 60 处链接，token=45字符+`=`，同页内每篇唯一 | E-004 |
| 2026-08-20 | cre | 离线结构分析本地样例文件（仅长度/字符集/前缀，未用于任何联网请求） | E-005 |
| 2026-08-20 | lead | 用户指令：停止用本地账号材料做联网试验，先列所需材料清单 | 暂停 ACT，待用户选路径 |
| 2026-09-02 | lead | 清理未使用的 replay/verify/lifecycle 等实验入口；补齐 URL/headers 脱敏、配置强校验、跨进程锁、原子限额检查、首次身份显式绑定和详情页 fail-closed 判定；建立隔离 Python/Playwright 环境并完成 12 项离线测试和 `about:blank` 浏览器启动检查 | 工具链可执行；未读取真实 Cookie/profile/capture，未访问小红书 |
| 2026-09-03 | lead | 用户指定 `test_a` 后执行首次在线实验；未绑定旧 profile 被原样备份，新 profile 注入 Cookie 后 `/api/sns/web/v2/user/me` 返回 HTTP 500；后续最小化首页探测为 HTTP 200，但无登录身份状态且页面请求 `/website-login/captcha` | 按停止条件在搜索前终止，未访问目标帖子；Cookie 已清除，失败 profile 已备份，待用户更新 Cookie 或确认身份接口变化 |
| 2026-09-03 | lead | 使用 `test_b`、`test_c` 各执行一次相同身份门禁；两者 Cookie header 均可解析且含 `web_session`，全新 profile 注入后 `/api/sns/web/v2/user/me` 均返回 HTTP 500；`test_b` 首页对照还出现二维码登录和 CAPTCHA 接口且无身份状态 | 两次均在搜索前终止，未访问目标帖子；三个账号的共同失败指向登录验证方式、共同 Cookie 导出条件或缺失 User-Agent/设备状态，而非单个账号异常 |
| 2026-09-03 | lead | 系统 Chrome 对照证明页面自身的签名 `/v2/user/me` 返回 HTTP 200，而 Playwright `context.request` 直调与 `/v1/user/selfinfo` 均返回 HTTP 500；据此把登录链改为“匿名设备初始化 → Cookie 覆盖 → 页面刷新 → 捕获页面签名身份响应”，并新增回归测试 | 根因确认为旧脚本绕过站点请求链；新实现完成 16 项离线测试，尚待一次受控在线验证 |
| 2026-09-03 | lead | 用 `test_a` 对新登录链进行受控在线验证；系统 Chrome成功完成匿名设备初始化与 Cookie 覆盖，并捕获页面自身的签名身份请求，但同时出现登录/CAPTCHA 响应和 HTTP 461 | 新链路在线验证有效并按规则停止；Cookie 未形成已登录会话，未搜索或访问目标帖子，不能通过放宽门禁继续 |
| 2026-09-03 | lead | 用户更新 `test_d` Cookie 后执行一次新登录链验证；Cookie header 结构检查通过，系统 Chrome完成设备初始化与覆盖注入，但页面仍产生登录/CAPTCHA相关 HTTP 200 响应 | 按停止条件在搜索前终止；本次未出现 461，但也未建立可接受的身份绑定，未访问目标帖子或生成 capture |
| 2026-09-03 | lead | 复核 `test_d` 停止逻辑后确认脚本把后台预加载的 HTTP 200 登录/CAPTCHA资源当成风险，并且在解析签名身份前提前失败；修复为“403/429/461 硬停止 → 解析页面签名身份 → 检查可见 CAPTCHA”，capture 阶段同步采用可见挑战判定 | HTTP 200 预加载不再误判；有效身份仍不能绕过可见挑战，17 项离线回归测试通过；`test_d` 的历史结果需在冷却后重新验证 |
| 2026-09-03 | lead | 修复同路径非 JSON 尾随响应覆盖有效身份候选的问题后首次重试 `test_d`；登录子进程超过预期导航时限仍未退出，且未写入身份绑定标记，人工中止整次运行 | 未进入搜索或生成 capture；确认总控脚本缺少子进程总时限，不能把本次挂起解释为账号已登录或平台限制 |
| 2026-09-03 | lead | 为总控登录、capture 和离线定位增加分步总时限、进程组清理与实时子进程输出，19 项离线回归测试通过；随后以全新隔离 profile 再次低频验证 `test_d` | 本次未挂起；页面签名 `/v2/user/me` 候选声明 JSON，但响应体无法解析为有效 JSON，按 fail-closed 门禁在搜索前停止，未访问目标帖子、未生成 capture，`test_d` 当前仍不能判定为可用 |
| 2026-09-03 | lead | 对身份门禁失败进行从头审计，并以三个只读 sub-agent 分别检查 session、总控/policy、capture/locator；合成复现确认“只解析最后一个身份响应”和 domain Cookie 未覆盖 host-only Cookie 两个脚本缺陷 | 因脚本缺陷，先前 `test_d` 结论修正为“当前脚本未能验证”，不能据此单独证明 Cookie 失效或账号受限；本轮未再次联网 |
| 2026-09-03 | lead | 修复精确 host Cookie 注入、可信 origin/签名身份 schema、staging profile 事务替换、原子 marker、进程组中断清理、严格 policy/每日尝试台账、目标限定脱敏、详情软 404、capture 回调、严格 locator 和 notes 白名单，并补齐离线回归测试 | 59 项离线测试通过，合成 Chrome 验证同名 host Cookie 被账户 Cookie 替换；未读取真实 Cookie/profile/capture，未运行在线实验。下一次在线结果必须重新取得，历史 D 失败不作为修复后结果 |
| 2026-09-03 | lead | 用户要求用修复后的完整流程重新测试 `test_d`；dry-run 通过后执行一次受控在线 staging 登录验证 | 页面出现实际可见的 CAPTCHA/安全挑战，脚本在身份绑定和搜索前硬停止；正式 profile 与身份 marker 均未创建，未访问目标帖子、未生成 capture/token。本结果证明当前会话被交互验证阻断，但不单独证明 Cookie 已过期 |
| 2026-09-03 | lead | 用户指定 `test_b` 后执行同样的首次绑定与对照：首页为 HTTP 200，但无身份状态，并请求二维码登录、`/website-login/captcha` 和 redcaptcha 接口 | 同样在搜索前终止，未访问目标帖子；证明现有 Cookie header 可解析但没有形成网页登录态，停止继续重试 |
| 2026-09-03 | lead | 增加显式 headed 人工验证等待模式；首次启动暴露 persistent Chrome 唯一初始页被关闭会触发 `TargetClosedError`，修复为保留一个 `about:blank` 中性页，62 项离线测试通过；随后重新启动 `test_d` | 窗口稳定显示二维码登录而非可直接恢复的 Cookie 登录态；用户当前无法扫码，故主动终止等待。未绑定身份、未搜索、未访问目标帖子、未生成 capture/token；该账号当日登录尝试额度已用完 |
| 2026-09-09 | lead | 读取用户引用的“实施方案-项目登录方式”对话，按其中 HTTP 携带 Cookie 的方向实现独立协议路线并设为默认；本地签名、只读身份探针、可选单页搜索及精确目标详情，浏览器路线改为显式对照；同步范围、方法及手册 | 82 项离线测试通过，含真实签名库的合成输入、Mock HTTP 单帖链路、身份/限制停止与脱敏落盘；依赖和语法检查通过。本轮未读取真实 Cookie、profile 或 HAR，未对小红书执行在线请求；协议可用性仍待实测，不能将代码完成写成账号已登录 |
| 2026-09-09 21:43 NZST | lead | 按用户的 Cookie 验证请求，沿用最后指定的 `test_d`；本地 dry-run 通过后，使用仓库外现有 Cookie 执行一次协议身份探针，仅请求 `GET /api/sns/web/v2/user/me` | HTTP 200、业务码 `-100`，脚本分类为 `SESSION_EXPIRED`；本地当日第 1 次登录尝试，未验证身份、未创建协议身份绑定、未启动浏览器、未搜索或访问帖子，无重试或切路。本结果不能证明账号永久受限或协议登录成功。脱敏记录位于已忽略的 `experiment/data/protocol-runs/test_d_20260909T214318_4ddbdb68.json` |
| 2026-09-09 23:18 NZST | lead | 用户指定 `test_e`；本地 dry-run 通过后，使用仓库外现有 Cookie 执行一次协议身份探针，仅请求 `GET /api/sns/web/v2/user/me` | HTTP 200、业务码 `0`，非 guest 身份与有效用户 ID 检查通过，结果 `PROTOCOL_IDENTITY_VERIFIED`；本地当日第 1 次登录尝试，以首次观测的身份摘要绑定 `test_e`。未启动浏览器、未搜索或访问帖子，无重试或切路；本结果证明本次 Cookie 协议身份验证成功，不代表浏览器登录或完整搜索/详情链路已通过。脱敏记录位于已忽略的 `experiment/data/protocol-runs/test_e_20260909T231831_1525da4f.json` |
| 2026-09-09 | lead | 按用户要求将 E 的成功方法、固定版本和证据边界同步至实验说明/手册、登录与 xsec 专题、研究索引、路线图和交接；列出身份复核后单帖 token 来源与详情验证，再做生成/下发数据流研究的顺序 | 仅读取既有脱敏报告；未读取 Cookie、profile 或 HAR，未新增线上请求、未修改实验脚本或 Core。登录方法与研究顺序统一见[协议说明](experiment/protocol-method.md)，不把身份成功写成完整采集或 token 生成算法已完成 |
| 2026-09-10 01:30 NZST | lead | 用户指定简单单图短文公开样本；E 的协议单帖流程先复核身份，再尝试一次搜索。依据脱敏报告复核既有运行结果，未再次发送重复请求 | 身份 GET 为 HTTP 200 / API 0，`identity_binding=matched`；随后搜索 POST 返回 HTTP 461，结果 `HTTP_RISK_STOP`。当日 login/capture 尝试各第 1 次，共两次请求，无详情请求、目标 capture 或 token/source；未复用用户 Chrome 会话或截图中的 token。停止后不重试或切路，不能把 461 归因为搜索未命中、Cookie 过期、确定的 CAPTCHA 或具体签名缺陷。报告：`experiment/data/protocol-runs/test_e_20260910T013022_86652a36.json` |
| 2026-09-10 17:19 NZST | lead | 用户在离线修复后要求再次尝试；同一 E 账号与公开样本 dry-run 通过后，执行一次 `client_revision=2` 协议运行 | 唯一身份 GET 返回 HTTP 200 / API -100，结果 `SESSION_EXPIRED`，未通过身份门禁；当日第 2 次登录尝试，未占 capture 尝试、未发搜索/详情或生成目标材料。诊断保留 JSON object、两个验证 header 均不存在、无命中固定消息提示；报告 `0600` 且被忽略。停止后未重试或切路，不能据此判断修复后的搜索可用或账号永久受限。报告：`experiment/data/protocol-runs/test_e_20260910T171950_c8960124.json` |
| 2026-09-10 19:42 NZST | lead | 用户单独批准先测试 `test_a`，仅验证身份并允许成功后首次绑定；本地 dry-run 通过后，执行一次 `client_revision=2` 身份探针 | 唯一身份 GET 返回 HTTP 200 / API -100，结果 `SESSION_EXPIRED`；当日第 1 次登录尝试，未创建 A 身份绑定、未搜索/读取详情、未生成 capture。诊断为 JSON object、两个验证 header 均不存在、无命中固定消息提示；报告 `0600` 且被忽略。停止后未重试 A 或测试 B/C/D，不将本次会话验证失败解释为账号永久受限。报告：`experiment/data/protocol-runs/test_a_20260910T194246_b8383c6f.json` |
| 2026-09-11 15:00 NZST | lead | 用户另行指定 `test_b`，按上一轮相同规则仅验证身份；本地 dry-run 通过后执行一次 `client_revision=2` 身份探针，成功才保存首次绑定 | 唯一身份 GET 返回 HTTP 200 / API -100，结果 `SESSION_EXPIRED`；当日第 1 次登录尝试，未创建 B 身份绑定、未搜索/读取详情、无 capture。诊断为 JSON object、两个验证 header 均不存在、无命中固定消息提示；报告 `0600` 且被忽略。停止后未重试或测试其他账号，不据此判断账号永久受限或其他 Cookie 的状态。报告：`experiment/data/protocol-runs/test_b_20260911T150035_cda036c5.json` |
| 2026-09-11 15:36 NZST | lead | 用户另行指定 `test_c`，按相同规则仅验证身份；本地 dry-run 通过后执行一次 `client_revision=2` 身份探针，成功才保存首次绑定 | 唯一身份 GET 返回 HTTP 200 / API -100，结果 `SESSION_EXPIRED`；当日第 1 次登录尝试，未创建 C 身份绑定、未搜索/读取详情、无 capture。诊断为 JSON object、两个验证 header 均不存在、无命中固定消息提示；报告 `0600` 且被忽略。停止后未重试或测试其他账号，不据此判断账号永久受限或具体失效原因。报告：`experiment/data/protocol-runs/test_c_20260911T153641_272197fb.json` |
| 2026-09-11 15:54 NZST | lead | 用户再次指定 `test_d`，按相同规则仅验证身份；本地 dry-run 通过后执行一次 `client_revision=2` 身份探针，成功才保存首次绑定 | 唯一身份 GET 返回 HTTP 200 / API -100，结果 `SESSION_EXPIRED`；当日第 1 次登录尝试，未创建 D 身份绑定、未搜索/读取详情、无 capture。诊断为 JSON object、两个验证 header 均不存在、无命中固定消息提示；报告 `0600` 且被忽略。停止后未重试或测试其他账号，不据此判断账号永久受限或具体失效原因。报告：`experiment/data/protocol-runs/test_d_20260911T155407_ff78daf2.json` |
| 2026-09-11 17:56 NZST | lead | 用户新提供并登记 `test_f` Cookie 后另行指定 F 实验；本地 dry-run 通过，执行一次 `client_revision=2` 协议身份探针 | 唯一身份 GET 返回 HTTP 200 / API 0，非游客身份和有效用户 ID 检查通过，结果 `PROTOCOL_IDENTITY_VERIFIED`；当日第 1 次登录尝试，保存 `first_observed` 摘要绑定。未启动浏览器、未搜索/读取详情、无 capture 或 token；未重试或测试其他账号。报告和绑定均为 `0600` 且被忽略，不把本次身份通过当作完整单帖链路成功，也不据此认定 A–E 必然自然过期。报告：`experiment/data/protocol-runs/test_f_20260911T175600_a3a36070.json` |
| 2026-09-11 19:51–19:52 NZST | lead | 用户另行批准 F 搜索；新增仅搜索开关以保证不读详情，119 项离线测试与 dry-run 通过后，对原批准公开样本执行一次 `client_revision=3` 身份复核及第一页搜索 | 身份 HTTP 200 / API 0、`matched`；搜索 HTTP 461 / API 0，验证相关响应头均存在，结果 `HTTP_RISK_STOP`。JSON object，无命中固定消息提示；不能由业务码 0 覆盖 HTTP 失败或推断验证形式/触发根因。共两次请求，当日 login 第 2 次、capture 第 1 次，无目标材料或详情，停止后未重试或切路。报告：`experiment/data/protocol-runs/test_f_20260911T195145_64fb196e.json`；规范结果见[协议说明](experiment/protocol-method.md#2026-09-11-f-仅搜索尝试身份通过搜索返回验证标志) |
| 2026-09-12 NZST | lead | 用户批准使用 Just One API 免费额度，并确认创建金额为 0 元的命名测试 Token；从既有导出列表选择图文、视频各一篇，V1/V6 详情各调用一次，随后下载返回媒体 | 两次均 HTTP 200 / code 0、各扣费 0 元，各剩余 4 次免费额度；8 张 1728 × 2304 WebP 与一个 720p、852.380 秒 MP4 实际保存并通过解码、长度与 SHA-256 检查。未使用小红书 Cookie、列表 token 或不限额 Default Token，无搜索、重试或 Core 接入；原站 461 实验仍停止。最小响应、正文和媒体位于忽略的私有目录，具体证据与边界见[双样本实测](experiment/protocol-method.md#2026-09-12-双样本详情实测) |
| 2026-09-12 18:45–18:57 NZST | lead | 用户要求寻找当前有效测试 Cookie 并使用 ReaJason/xhs 搜索；F 预检通过后停止筛选。新增固定 SDK 受控入口，135 项离线测试通过，再用 SDK 身份方法与原生六字段搜索方法各一次 | F 两次身份 GET 均 HTTP 200 / API 0、摘要匹配；SDK 搜索接入现有本地签名后仍 HTTP 461 / API 0，验证相关响应头存在，立即停止。未测试 A–E，无详情、capture、token 保存或重试；不代表作者浏览器签名方案的结果。脱敏报告为 `experiment/data/reajason-runs/test_f_20260912T185654_80dcf2af.json`，规范结果见[搜索对照](experiment/protocol-method.md#2026-09-12-reajasonxhs-搜索对照) |
| 2026-09-12 22:33–22:34 NZST | lead | 用户补充已网页登录小号的 16 项 Cookie，登记为 G 后授权自有脚本身份＋搜索；56 项相关离线测试与 dry-run 通过，未改请求或签名代码 | 身份与搜索均 HTTP 200 / API 0，首次观测身份绑定，第一页精确发现批准目标及其 token，结果 `PROTOCOL_SEARCH_VERIFIED`。source 来自搜索上下文，不是响应返回；私有最小 capture 已核验，无详情、媒体、重试或旧账号复测。报告为 `experiment/data/protocol-runs/test_g_20260912T223350_732901e0.json`，规范结果见[G 实测](experiment/protocol-method.md#2026-09-12-g-自有脚本身份及搜索成功) |
| 2026-09-13 12:00 NZST | lead | 用户要求检查 F 常用只读功能；新增身份→本账号资料→本账号发帖列表的三次 GET 受限入口，离线回归及 dry-run 通过后执行一次 | 身份 HTTP 200 / API -100，`SESSION_EXPIRED`；共一次请求即停止，资料/发帖列表未请求，未搜索、读取详情、重试或换号。报告为 `experiment/data/capability-runs/test_f_20260913T120012_75c248b4.json`，权限与字段核验通过；不能由本次失效反向解释前日搜索 461。规范结果见 [F 能力检查](experiment/protocol-method.md#2026-09-13-f-常用只读功能检查) |
| 2026-09-13 13:46–13:49 NZST | lead | 用户要求逐个复核 A–F，仅在全部失败时删除 Cookie，并将 G 标为“小号”；各一次身份 GET 后核对六份报告与冻结输入 | 六个均 HTTP 200 / API -100、无验证头/风险提示；13:49:41 在锁内清空外部配置中 A–F 的 Cookie，保留账号元数据、绑定与历史，不留旧凭据备份。G 原值未改，仅加“小号”标签；本轮未请求 G、搜索或详情。配置加载与缺少凭据时拒绝联网验证通过，证据见[复核与清理](experiment/protocol-method.md#2026-09-13-af-身份复核与条件清理) |
| 2026-09-14 15:02–15:06 NZST | lead | 用户明确授权小号 G 与既定实验帖的无 `xsec_token` 链接测试；先执行现有协议身份核验，再以 XHS-Downloader 固定版本网页请求头和受控 HTTP 传输请求 `/explore/` 链接 | 身份 HTTP 200 / API 0、摘要匹配；网页 HTTP 302，指向同站 `/404/…`，`error_code=300031`。未跟随跳转，停止后续 `/discovery/item/` 请求；无搜索、历史 token 复用、真实帖子解析或媒体下载。原版 Converter 仅完成合成输入检查，未运行完整 CLI。结果与方法边界见 [XHS-Downloader 实测补充](../../../projects/xhs-downloader/review.md#2026-09-14-实测补充小号-cookie--无-token-链接)，不能据此判定所有帖子都必须带 token。 |
| 2026-09-14 15:28–15:34 NZST | lead | 用户提供同一实验帖的完整 token 链接，明确要求不使用 Cookie；使用固定版本 Converter、Explore、Image、Download 与受控无 Cookie 传输完成单帖下载 | 网页 HTTP 200，目标 ID 精确匹配；图片 HTTP 206，保存 JPEG 1179 × 1548、97,041 字节，长度、完整 Content-Range、传输/落盘 SHA-256 和解码验证通过。共两次 GET，均无 Cookie/Authorization；无账号配置读取、身份接口、搜索、浏览器、重试或重定向。保存正文、详情 JSON、图片和脱敏报告，结果 `COMPLETE_COOKIE_FREE_LINK_DOWNLOAD`；见 [无 Cookie 下载实测](../../../projects/xhs-downloader/review.md#2026-09-14-实测补充完整链接不使用-cookie下载成功)。 |
