# 本实验局部运行规则，不是产品政策。
request_policy:
  min_delay_seconds: 5
  max_delay_seconds: 15
  max_login_attempts_per_account_per_day: 3
  max_capture_runs_per_account_per_day: 3
  max_search_scrolls_per_run: 3
  search_settle_seconds: 5
  default_timeout_seconds: 45
